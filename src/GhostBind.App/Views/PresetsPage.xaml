<Page x:Class="GhostBind.App.Views.PresetsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Presets">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <TextBlock Text="Game Presets" FontSize="28" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock TextWrapping="Wrap" Margin="0,4,0,16"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Text="One-click starting points for specific games. Applying a preset overwrites the current profile's stick + trigger settings (button mapping is left alone). Tweak from there." />
            <TextBlock TextWrapping="Wrap" Margin="0,0,0,24" FontStyle="Italic"
                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" FontSize="11"
                       Text="Tip: create a fresh profile for each game first (Profiles page), switch to it, THEN apply the preset. That way you can A/B between vanilla and tuned." />

            <ItemsControl x:Name="PresetList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Padding="20" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16"
                                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                    <TextBlock Text="{Binding Tagline}" Margin="0,2,0,8"
                                               Foreground="{DynamicResource SystemAccentColorBrush}" FontSize="11" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Notes}" TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" />
                                </StackPanel>
                                <ui:Button Grid.Column="1" Content="Apply" Appearance="Primary"
                                           VerticalAlignment="Top" Margin="16,0,0,0"
                                           Tag="{Binding Name}" Click="OnApplyClick" />
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock x:Name="StatusLine" Margin="4,8,0,0"
                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
        </StackPanel>
    </ScrollViewer>
</Page>
